http:
  port: 8091

grpc:
  port: 9091

redis:
  addr: "118.24.164.222:6379"
  password: "kelab666"
  db: 2
  pool_size: 50
  dial_timeout_seconds: 5
  read_timeout_seconds: 3
  write_timeout_seconds: 3

captcha:
  ttl_seconds: 120
  width: 320
  height: 180
  graph_size_min: 52
  graph_size_max: 64
  slider_tolerance: 8

token:
  ttl_seconds: 300
  secret: "CHANGE_ME"

# Nacos服务注册配置
nacos:
  enable: true # 是否启用Nacos服务注册
  server_addr: "118.24.164.222:8848" # Nacos服务器地址
  namespace: "" # 命名空间（空字符串表示public命名空间）
  service_name: "captcha-service" # 服务名称（用于服务发现）
  group_name: "DEFAULT_GROUP" # 服务分组
  cluster_name: "DEFAULT" # 集群名称
  weight: 1.0 # 服务权重（用于负载均衡）
  metadata: # 服务元数据
    version: "1.0.0"
    protocol: "http"
    context-path: "/api/v1"
    env: "prod"
